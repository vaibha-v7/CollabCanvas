Docker setup for CollabCanvas

Quick start
1) Update backend env vars in docker-compose.yml
2) Run: docker compose up --build

Ports
- Backend: http://localhost:5000
- Frontend: http://localhost:5173
